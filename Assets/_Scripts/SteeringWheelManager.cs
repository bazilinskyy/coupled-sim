using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SteeringWheelManager : MonoBehaviour
{
   /* private Transform player;
    public Transform startingPosition;
    public GameObject steeringWheelPrefab;
    private GameObject steeringWheel;
    // Start is called before the first frame update
    void Start()
    {
        player = GameObject.FindGameObjectWithTag("Player").transform;

        //Spawn steeringwheel beneath plane.
        steeringWheel = Instantiate(steeringWheelPrefab);
        steeringWheel.transform.position  = -Vector3.up * 10;

    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.X)) { GetComponent<MySceneLoader>().AddTargetScene(); }
        if (Input.GetKeyDown(KeyCode.Z)) { CalibrateHands(); }
    }

    void CalibrateHands()
    {
        steeringWheel.transform.position = Vector3.zero;
    }*/
}
